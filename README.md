# 🎬 Кинотеатр "Сфера"

Одностраничное веб-приложение для бронирования и покупки билетов в кинотеатр с синхронизацией в реальном времени между пользователями.

## Возможности

- 📅 Просмотр сеансов на неделю вперед
- 🔍 Фильтры по названию, дате, жанру и цене
- 🎫 Интерактивный зал с тремя категориями мест:
  - **Эконом** (ряд 1) - 200₽
  - **Стандарт** (ряды 2-10) - 350₽
  - **Премиум** (места 4-7 в рядах 4-6, центр зала) - 500₽
- ⏱️ Бронирование мест с таймером (3 минуты)
- 💳 Симуляция оплаты билетов
- 🔄 Синхронизация состояния между всеми пользователями в реальном времени
- 🛠️ Девопс: сброс купленных мест (для тестирования)

## Настройка Firebase

### 1. Создайте проект Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Добавить проект"
3. Введите название проекта (например, "cinema-sphere")
4. Следуйте инструкциям для создания проекта

### 2. Настройте Firestore Database

1. В левом меню выберите "Firestore Database"
2. Нажмите "Создать базу данных"
3. Выберите режим "Начать в тестовом режиме" (для разработки)
4. Выберите регион (например, "europe-west")

### 3. Получите конфигурацию

1. Перейдите в "Настройки проекта" (иконка шестеренки)
2. Прокрутите вниз до раздела "Ваши приложения"
3. Нажмите на иконку веб-приложения `</>`
4. Зарегистрируйте приложение с названием (например, "Cinema Web")
5. Скопируйте конфигурационные данные

### 4. Обновите файл firebase-config.js

Откройте файл `firebase-config.js` и замените значения на ваши данные:

```javascript
const firebaseConfig = {
    apiKey: "ваш-api-key",
    authDomain: "ваш-project-id.firebaseapp.com",
    projectId: "ваш-project-id",
    storageBucket: "ваш-project-id.appspot.com",
    messagingSenderId: "ваш-messaging-sender-id",
    appId: "ваш-app-id"
};
```

### 5. Настройте правила безопасности Firestore (опционально)

Для продакшена рекомендуется настроить правила:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{sessionId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
```

## Развертывание на GitHub Pages

### Вариант 1: Через веб-интерфейс GitHub

1. Создайте новый репозиторий на GitHub
2. Загрузите все файлы проекта
3. Перейдите в Settings → Pages
4. В разделе "Source" выберите ветку `main` и папку `/ (root)`
5. Нажмите "Save"
6. Ваш сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория/`

### Вариант 2: Через командную строку

```bash
# Инициализируйте git репозиторий (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: Cinema booking app"

# Добавьте удаленный репозиторий
git remote add origin https://github.com/ваш-username/название-репозитория.git

# Отправьте код
git push -u origin main
```

Затем активируйте GitHub Pages в настройках репозитория.

## Локальное тестирование

Для локального тестирования используйте любой HTTP-сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (с установленным http-server)
npx http-server

# VS Code - расширение Live Server
# Откройте index.html и нажмите "Go Live"
```

Затем откройте браузер и перейдите на `http://localhost:8000`

## Структура проекта

```
cinema-sphere/
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # Логика приложения
├── firebase-config.js  # Конфигурация Firebase
└── README.md           # Документация
```

## Использование

### Для пользователей:

1. **Главная страница:** просмотр доступных сеансов
2. **Фильтры:** поиск нужного сеанса
3. **Выбор сеанса:** клик по карточке сеанса
4. **Выбор мест:** клик по свободным местам
5. **Бронирование:** нажмите "Забронировать"
6. **Оплата:** нажмите "Оплатить" в течение 3 минут

### Для администраторов:

- Кнопка "Сбросить купленные места" - сбрасывает все купленные билеты (забронированные остаются)

## Технические детали

### Категории мест:

- **Ряд 1:** Эконом (зеленый) - 200₽
- **Ряды 2-10:** Стандарт (синий) - 350₽
- **Ряды 4-6, места 4-7:** Премиум (оранжевый) - 500₽

### Статусы мест:

- **Доступно:** можно выбрать
- **Выбрано:** выбрано текущим пользователем (фиолетовый)
- **Забронировано:** забронировано другим пользователем, таймер активен (желтый)
- **Куплено:** оплачено, недоступно (серый)

### Таймер брони:

- При бронировании запускается таймер на 3 минуты
- Если не оплатить в течение 3 минут, бронь снимается
- Закрытие окна оплаты НЕ снимает бронь

## Особенности синхронизации

- Все изменения (бронирования, покупки) синхронизируются в реальном времени
- Несколько пользователей могут одновременно просматривать один сеанс
- Забронированные места видны всем пользователям
- Купленные места недоступны для покупки

## Устранение неполадок

### Сеансы не загружаются

- Проверьте, что Firebase правильно настроен
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что `firebase-config.js` содержит правильные данные

### Места не обновляются

- Проверьте правила безопасности Firestore
- Убедитесь, что приложение имеет доступ к Firestore

### GitHub Pages не работает

- Убедитесь, что репозиторий публичный
- Проверьте настройки Pages в настройках репозитория
- Подождите несколько минут после активации Pages

## Лицензия

MIT

## Автор

Cinema Sphere Team
